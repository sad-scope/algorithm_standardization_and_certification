import * as colors from "@colors/colors/safe";

export const MINIMUM_ARRAY_LENGTH = 2;

export const NO_ELEMENTS_BETWEEN = `${colors.magenta(
  "Отсутствуют элементы между максимальным и минимальным элементом: "
)} ${colors.cyan("Произведение равно 0")}`;
